PATCH 30.2.0A - JSONL tail reader everywhere

Scope:
- Replace local JSONL tail readers that loaded entire files with wrappers around `core.jsonl_utils.iter_jsonl_tail`.
- Keep callers and event filtering behavior intact.
- Add a regression test preventing local tail helpers from using `read_text().splitlines()` again.

Files:
- trading_bot/core/jsonl_utils.py (existing shared helper)
- trading_bot/core/paper_once_runner_footer.py
- trading_bot/core/paper_unlock_supervised_execution.py
- trading_bot/core/lsr_v2_* affected tail-reader modules
- trading_bot/test_runtime_risk_io_hardening.py
- docs/patch_reports/PATCH_30_2_0A_MANIFEST.txt
- trading_bot/docs/PROMPT_30_2_0A_JSONL_TAIL_READER_EVERYWHERE_PATCH_REPORT.md

Safety:
- Read-only behavior change for diagnostics.
- No broker calls.
- No order submit or close.
- No runtime state mutation.
- No live/testnet/exchange enablement.

Validation:
- AST parse for `trading_bot/core/*.py` -> PASS.
- Regression scan for bad local JSONL tail helpers -> PASS.
- Manual LSR-v2/paper test runner -> PASS.
